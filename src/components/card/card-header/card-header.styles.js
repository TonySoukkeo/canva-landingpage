import styled from "styled-components";

export const Header = styled.h1`
  text-align: left;
  color: black;
  font-weight: 700;
  font-size: 2.4rem;
  letter-spacing: -0.03rem;
  line-height: 1.3;
`;
